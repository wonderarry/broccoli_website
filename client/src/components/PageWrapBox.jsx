import { Box } from "@mui/material";
import { styled } from "@mui/system";



const PageWrapBox = styled(Box)({
    height: '100%',
    display: 'flex',
    justifyContent: 'space-between',
    flexDirection: 'column'
})

export default PageWrapBox;